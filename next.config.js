/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['*'], // Allow loading images from any domain
      },
    output: 'export',

}

module.exports = nextConfig
